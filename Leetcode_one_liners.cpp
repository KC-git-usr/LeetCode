//Leetcode one-liners

nums.size()
return *max_element(nums.begin(), nums.end());


int arr[] = { 1, 5, 8, 9, 6, 7, 3, 4, 2, 0 };
int n = sizeof(arr) / sizeof(arr[0]);



//count freq of elements (used in counting sort)
    // Store count of each character
    for (i = 0; arr[i]; ++i)
        ++count[arr[i]];